apiVersion: v1
kind: Service
metadata:
  name: luxe-react-service
spec:
  type: NodePort
  selector:
    app: luxe-react
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
