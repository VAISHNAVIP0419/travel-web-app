apiVersion: v1
kind: Service
metadata:
  name: luxe-frontend-service
  labels:
    app: luxe-frontend
spec:
  selector:
    app: luxe-frontend
  ports:
    - port: 80
      targetPort: 80
  type: LoadBalancer